# Server Configuration
PORT=3000
NODE_ENV=development

# Database
DATABASE_URL="postgresql://user:password@localhost:5432/kof_assistant?schema=public"
# O para SQLite (más fácil para desarrollo):
# DATABASE_URL="file:./dev.db"

# JWT Secret (genera uno único con: openssl rand -base64 32)
JWT_SECRET=your_super_secret_key_change_this_in_production
JWT_EXPIRES_IN=7d

# RAG API URL (tu backend Python)
RAG_API_URL=http://localhost:8000

# CORS
CORS_ORIGIN=*
